The test file for the propositional logic is fine to be ru on the command line using:
python main.py -f test_propositional.txt

However, the individual first order logics need to be copy and pasted onto the command line, like:
python main.py "copy pasted logic"
